# Phase 5.7—5.8 Verification

> Date: 2026-08-25
> Baseline HEAD: `772948a6f6fe28b353007b009658e277f07475ed`
> Scope: interview documentation, local display materials and a bounded admin visual polish.

## Phase 5.7

[`three-minute-demo.md`](../interview/three-minute-demo.md) now contains:

- a synthetic-only local startup and shutdown procedure;
- a 0:00—3:00 timeline with narration and code/report evidence;
- the minimum route, fallback and operations demonstrations;
- a no-secret recording checklist and an explicit production boundary.

The initial documentation state did not contain a video file and did not claim
that a recording had been made. A replacement candidate was generated with
Playwright against the same synthetic stack and copied to
[`gaokao-agent-demo.webm`](../assets/gaokao-agent-demo.webm). The current file is
3,067,145 bytes with SHA-256
`73D88DDC7F3492FF528CDC29822111C8AFC9D047344907E4F6078EBDA28BFB78` and begins
with the WebM/EBML header `1A 45 DF A3`. A temporary localhost HTTP server and
Chromium media probe reported `contentType=video/webm`, `readyState=4`,
`duration=134.04s`, `videoWidth×videoHeight=800×600`, `audioTracks=0`, and
`mediaError=null`. Playback checkpoints around 1:16 (styled admin with
synthetic catalog data), 1:48 (version JSON) and 2:02 (Skill JSON) were visually inspected;
the admin checkpoint no longer shows unstyled HTML. The four recorded chapters
are the home page, structured fallback chat, operations admin, and
`/version`/`/api/chat/skills` API evidence. The candidate is a silent screen
recording, and the sidecar subtitle file
[`gaokao-agent-demo.vtt`](../assets/gaokao-agent-demo.vtt) now provides the
reviewable narration text; optional voiceover remains a later packaging choice.

The VTT cues were aligned against the formal WebM with Chromium seek sampling:
the major visual transitions occur around 00:32 (home to chat), 01:13 (chat to
admin), 01:45 (admin to version JSON), and 01:51 (version to Skill JSON). The
last cue ends at 02:14.040, matching the measured 134.04-second duration.

## Phase 5.8

[`interview-qa.md`](../interview/interview-qa.md) contains 15 interview questions
covering Agent routing, framework choice, fallback, evaluation, trace privacy,
server-side entitlements, WeChat security, URL safety, retention, operations,
testing, production gaps, data risk and cost. Each answer points to an
implementation file, design specification or verification report and calls out
known limits where the evidence is local-only.

## Validation

- README links to both interview artifacts and the replacement video candidate.
- README and the Demo script link to the local video candidate; it uses only
  synthetic configuration and local example data.
- Relative Markdown links pass the repository link check.
- Secret-pattern scan passes; no real credential was added.
- `git diff --check` passes.
- The admin dashboard now has a bounded `admin-shell` visual layer; the focused
  Web regression passes 21/21 and the real local page reports the expected root
  class before screenshot capture.
- The current project verifier completed successfully after these documentation
  updates: data validation found 2 schools and 4 majors; API had 205 passing
  tests with 85% total coverage; Web had 129 passing tests with
  86.64% statements / 84.21% branches / 72.34% functions; Web typecheck and
  production build passed. Existing deprecation, raw `<img>`, and ESLint-plugin
  warnings remain documented by the verifier.
- CI-aligned local gates also passed: Ruff, Black from the API virtual
  environment (`68 files would be left unchanged`), and Web ESLint (0 errors,
  3 existing raw-`<img>` warnings).
