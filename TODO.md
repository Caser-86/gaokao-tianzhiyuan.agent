# TODO

> 本文件只记录未完成任务。已完成的阶段、验证数字和过程决策保留在 `docs/verification/`、`PROJECT_REVIEW.md` 和 `PLAN.md`，不在这里重复维护。

## P0 — 公开发布前阻塞项

- [ ] 在私有环境执行真实 Provider 成对评测：固定问题、相同预算、实际返回模型、token/cost、失败样本和人工判定均需留证；不能把 `ark-code-latest` 路由别名当作真实质量结论。
- [ ] 将请求限流和日预算从进程内状态替换为多 worker 可共享的存储，并补并发、重启和超限回归。
- [ ] 完成一次真实 Docker runtime / HTTPS / post-deploy smoke / rollback 演练，记录发布版本和数据库备份恢复结果。
- [ ] 配置外部日志轮转、敏感字段过滤、告警阈值和故障定位入口；当前应用只负责输出脱敏 trace，不负责生产日志生命周期。

## P1 — 核心安全与产品闭环

- [ ] 增加账号级认证、session 撤销、设备管理和长期主体生命周期；明确 guest session 到账号的迁移边界。
- [ ] 为微信公众号补充高并发限流/WAF 或等价入口保护，并验证明文/AES 回调在多实例部署下的幂等语义。
- [ ] 继续加固外部 URL/媒体出站：DNS rebinding 防护、响应 MIME/内容校验、逐跳解析约束和出站速率控制。
- [ ] 建立真实数据接入流程：来源许可、抓取/更新时间、适用年份与地区、官方/二手分类、负责人和人工发布审核。

## P2 — 可测量的工程优化

- [ ] 先建立真实数据规模、并发、延迟、token/cost 和 fallback 基线，再决定缓存、模型分层或检索升级；不要凭感觉引入重型依赖。
- [ ] 为 API/Web 质量门禁补充覆盖率阈值和长期趋势记录，并降低现有依赖/Node action/`<img>` 警告。
- [ ] 按审核、媒体、精选轮换继续拆分后台大组件，只在有回归收益时改动，不进行一次性重构。

## P3 — 可选展示增强

- [ ] 为三分钟 Demo 补录人工旁白，并在发布前重新做 secret、个人信息、域名和模型配置复核。
- [ ] 只有真实非结构化样本证明 SQL-first 不足时，才设计轻量检索/RAG 方案并更新评测基线。

## 执行顺序

```text
真实 Provider 评测
        |
共享预算 + 出站安全 + 身份生命周期
        |
Docker runtime / HTTPS / 备份 / smoke / rollback
        |
监控告警与按指标优化
```

每项完成后应新增带日期的验证记录，并同步 `docs/verification/latest.json`；没有外部证据的事项保持“未确认”，不改写成“已完成”。
